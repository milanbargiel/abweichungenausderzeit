{% macro textualteaser(title, link) %}
  <div class="grid-item col-sm-6 col-md-4">
    <a class="teaser-header" href="#">{{ title | safe }}</a>
    <div>
      {{ caller() }}
    </div>
    <a class="teaser-readmore" href="{{ link }}">WEITERLESEN</a>
  </div>
{% endmacro%}